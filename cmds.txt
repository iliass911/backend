git init

# Add all files
git add .

# Commit your files
git commit -m "Initial commit"

# Add your GitHub repository as remote (replace YOUR_USERNAME)
git remote add origin https://github.com/iliass911/frontend
# Push your code
git push -u origin main












# Initialize git
git init

# Add all files
git add .

# Commit your files
git commit -m "Initial commit"

# Add your GitHub repository as remote (replace YOUR_USERNAME)
git remote add origin https://github.com/iliass911/backend

# Push your code
git push -u origin main